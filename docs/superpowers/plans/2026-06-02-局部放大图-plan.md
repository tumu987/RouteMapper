# 局部放大图（Zoom Inset）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为路线图生成引擎添加自动局部放大功能——当相邻城市距离过近时，自动在空白区域生成放大示意图。

**Architecture:** 在现有 `加载→底图→放置→松弛→渲染` 管道中插入检测和放大渲染环节。检测在加载后执行；放置用 LayoutEngine 螺旋搜索；渲染在行程表之后，使用 matplotlib inset_axes 创建独立放大区。

**Tech Stack:** Python 3.11+, Matplotlib + Cartopy（与现有项目一致）

---

## 文件结构

| 文件 | 角色 | 改动类型 |
|------|------|----------|
| `config.py` | 新增阈值/外扩常量 | 修改：2 行 |
| `generate.py` | 检测、分组、范围计算、放置、管道集成 | 修改：+150 行 |
| `renderer.py` | 放大图内容渲染 + 主图标记渲染 | 修改：+100 行 |
| `layout.py` | 不改动（复用现有碰撞 API） | 无 |

---

### Task 1: config.py — 新增常量

**Files:** Modify `config.py`

- [ ] **Step 1: 在 config.py 末尾添加常量**

```python
# ── 局部放大图 ──
INSET_THRESHOLD = 0.02       # 触发阈值：对角线比例
INSET_PADDING = 1.5          # 放大区域外扩倍率
```

修改位置：在文件末尾，`get_city_index` 函数之后。

- [ ] **Step 2: 验证导入**

```bash
python -c "from config import INSET_THRESHOLD, INSET_PADDING; print(INSET_THRESHOLD, INSET_PADDING)"
```

预期输出: `0.02 1.5`

- [ ] **Step 3: Commit**

```bash
git add config.py
git commit -m "feat(config): 添加局部放大图阈值常量"
```

---

### Task 2: generate.py — 检测过近城市对

**Files:** Modify `generate.py`

- [ ] **Step 1: 在 generate.py 中添加 `_detect_close_pairs` 函数**

放在 `_build_itinerary_lines` 函数之前（辅助函数区）：

```python
def _detect_close_pairs(cities: list, segments: list,
                        threshold: float) -> list:
    """检测 segments 中距离过近的相邻城市对。

    Args:
        cities: 城市列表
        segments: 路线段列表
        threshold: 距离阈值（地图坐标）

    Returns:
        [(from_index, to_index), ...] 过近的相邻城市对
    """
    lons = [c["lon"] for c in cities]
    lats = [c["lat"] for c in cities]
    diagonal = math.hypot(max(lons) - min(lons), max(lats) - min(lats))
    threshold_dist = diagonal * threshold

    close = []
    for seg in segments:
        fi, ti = seg["from_index"], seg["to_index"]
        dist = math.hypot(
            cities[fi]["lon"] - cities[ti]["lon"],
            cities[fi]["lat"] - cities[ti]["lat"],
        )
        if dist < threshold_dist:
            close.append((fi, ti))
    return close
```

- [ ] **Step 2: 验证函数可导入**

```bash
python -c "from generate import _detect_close_pairs; print('OK')"
```

预期输出: `OK`

- [ ] **Step 3: Commit**

```bash
git add generate.py
git commit -m "feat: 添加过近城市对检测函数"
```

---

### Task 3: generate.py — 连通分量分组

**Files:** Modify `generate.py`

- [ ] **Step 1: 在 `_detect_close_pairs` 下方添加 `_group_connected_pairs`**

```python
def _group_connected_pairs(pairs: list) -> list:
    """将过近城市对按连通关系聚合为城市簇。

    使用并查集（union-find）。A-B 且 B-C → 合并为一个簇 {A, B, C}。

    Args:
        pairs: [(from_index, to_index), ...]

    Returns:
        [{idx1, idx2, idx3}, ...] 每个集合是一个需要放大的城市簇
    """
    parent = {}

    def find(x):
        if x not in parent:
            parent[x] = x
        if parent[x] != x:
            parent[x] = find(parent[x])
        return parent[x]

    def union(x, y):
        px, py = find(x), find(y)
        if px != py:
            parent[px] = py

    for i, j in pairs:
        union(i, j)

    groups = {}
    for i, j in pairs:
        root = find(i)
        groups.setdefault(root, set()).add(i)
        groups.setdefault(root, set()).add(j)

    return list(groups.values())
```

- [ ] **Step 2: 交互验证**

```bash
python -c "
from generate import _group_connected_pairs
pairs = [(0,1), (1,2), (4,5)]
print(_group_connected_pairs(pairs))
"
```

预期输出: `[{0, 1, 2}, {4, 5}]`（顺序可能不同）

- [ ] **Step 3: Commit**

```bash
git add generate.py
git commit -m "feat: 添加过近城市对连通分量分组"
```

---

### Task 4: generate.py — 计算放大范围

**Files:** Modify `generate.py`

- [ ] **Step 1: 添加 `_compute_zoom_extent` 函数**

```python
def _compute_zoom_extent(cluster_indices: set, cities: list,
                         padding: float) -> list:
    """计算放大区域范围。

    收集簇内所有城市 + 景点坐标的 bbox，按 padding 倍率外扩。
    宽高比 clamp 到 [0.4, 2.5] 防止极端扁平。

    Args:
        cluster_indices: 城市索引集合
        cities: 城市列表
        padding: 外扩倍率（>1 扩大，<1 缩小）

    Returns:
        [lon_min, lon_max, lat_min, lat_max]
    """
    all_lons = []
    all_lats = []
    for idx in cluster_indices:
        c = cities[idx]
        all_lons.append(c["lon"])
        all_lats.append(c["lat"])
        for attr_key in ("attractions_primary", "attractions_secondary"):
            for a in c.get(attr_key, []):
                try:
                    all_lons.append(float(a["lon"]))
                    all_lats.append(float(a["lat"]))
                except (ValueError, TypeError):
                    pass

    if not all_lons:
        return None

    lon_center = (min(all_lons) + max(all_lons)) / 2
    lat_center = (min(all_lats) + max(all_lats)) / 2
    lon_span = (max(all_lons) - min(all_lons)) * padding
    lat_span = (max(all_lats) - min(all_lats)) * padding

    # 确保最小跨度（至少 0.3°）
    lon_span = max(lon_span, 0.3)
    lat_span = max(lat_span, 0.2)

    # 宽高比 clamp
    avg_lat = sum(all_lats) / len(all_lats)
    cos_lat = math.cos(math.radians(avg_lat))
    ratio = (lon_span * cos_lat) / lat_span if lat_span > 0 else 1.0
    if ratio > 2.5:
        lon_span = lat_span * 2.5 / cos_lat
    elif ratio < 0.4:
        lat_span = lon_span * cos_lat / 0.4

    return [
        round(lon_center - lon_span / 2, 3),
        round(lon_center + lon_span / 2, 3),
        round(lat_center - lat_span / 2, 3),
        round(lat_center + lat_span / 2, 3),
    ]
```

- [ ] **Step 2: 交互验证**

```bash
python -c "
from generate import _compute_zoom_extent
cities = [{'name': 'A', 'lon': 87.0, 'lat': 48.6, 'attractions_primary': [], 'attractions_secondary': []},
          {'name': 'B', 'lon': 87.1, 'lat': 48.5, 'attractions_primary': [], 'attractions_secondary': []}]
ext = _compute_zoom_extent({0,1}, cities, 1.5)
print(ext)
"
```

预期输出: 一个以(87.05, 48.55)为中心、外扩后的 extent

- [ ] **Step 3: Commit**

```bash
git add generate.py
git commit -m "feat: 添加放大区域范围计算函数"
```

---

### Task 5: generate.py — 放大图放置（螺旋搜索）

**Files:** Modify `generate.py`

- [ ] **Step 1: 添加 `_place_zoom_inset` 函数**

```python
def _place_zoom_inset(layout: LayoutEngine,
                      extent_zoom: list,
                      main_extent: list,
                      crs_cos: float) -> tuple:
    """螺旋搜索空白区域放置放大图。

    从放大区域的中心出发，螺旋式向外搜索，直到找到
    不与已有元素冲突的位置。

    Args:
        layout: 布局引擎
        extent_zoom: 放大区域 [lon_min, lon_max, lat_min, lat_max]
        main_extent: 主图范围
        crs_cos: 纬度余弦补偿

    Returns:
        (center_lon, center_lat, half_w, half_h) 或 (None, None, 0, 0)
    """
    # 放大图占主图约 25% 宽度
    inset_fig_w = 0.25
    map_lon_span = main_extent[1] - main_extent[0]
    map_lat_span = main_extent[3] - main_extent[2]

    zoom_lon_span = extent_zoom[1] - extent_zoom[0]
    zoom_lat_span = extent_zoom[3] - extent_zoom[2]

    # 放大图宽高按 zoom 区域比例
    zoom_ratio = (zoom_lat_span / (zoom_lon_span * crs_cos)) if zoom_lon_span > 0 else 1.0
    inset_fig_h = inset_fig_w * zoom_ratio
    # clamp
    inset_fig_h = max(0.15, min(0.35, inset_fig_h))

    # 转换为地图坐标尺寸
    inset_hw = (inset_fig_w * map_lon_span / 0.98) / 2
    inset_hh = (inset_fig_h * map_lat_span / 0.98) / 2

    # 螺旋搜索
    center_lon = (extent_zoom[0] + extent_zoom[1]) / 2
    center_lat = (extent_zoom[2] + extent_zoom[3]) / 2

    for step in range(1, 20):
        radius = 0.3 + step * 0.25
        for ang_idx in range(8):
            angle = math.radians(ang_idx * 45 + step * 15)
            cx = center_lon + radius * math.cos(angle)
            cy = center_lat + radius * math.sin(angle)

            # 不超出主图范围
            if not (main_extent[0] + inset_hw < cx < main_extent[1] - inset_hw):
                continue
            if not (main_extent[2] + inset_hh < cy < main_extent[3] - inset_hh):
                continue

            if layout.is_position_clear(cx, cy, inset_hw, inset_hh,
                                        margin=0.05, my_kind="inset"):
                # 注册到布局，防止其他元素覆盖
                layout.place(cx, cy, inset_hw, inset_hh,
                            f"inset_{center_lon:.2f}_{center_lat:.2f}")
                return cx, cy, inset_hw, inset_hh

    # 回退：右上角
    cx = main_extent[1] - inset_hw - 0.3
    cy = main_extent[3] - inset_hh - 0.3
    layout.place(cx, cy, inset_hw, inset_hh,
                f"inset_fb_{center_lon:.2f}")
    return cx, cy, inset_hw, inset_hh
```

- [ ] **Step 2: 验证语法**

```bash
python -c "from generate import _place_zoom_inset; print('OK')"
```

预期输出: `OK`

- [ ] **Step 3: Commit**

```bash
git add generate.py
git commit -m "feat: 添加放大图螺旋搜索放置函数"
```

---

### Task 6: renderer.py — 放大图内容渲染

**Files:** Modify `renderer.py`

- [ ] **Step 1: 在 renderer.py 末尾添加 `render_zoom_inset_content`**

```python
# ── 局部放大图 ──

def render_zoom_inset_content(ax: Any, extent_zoom: list,
                               cluster_indices: set,
                               cities: list, segments: list,
                               day_colors: dict) -> None:
    """在给定的 axes 上渲染放大图内容。

    包含：底图 + 省界 + 簇内城市节点 + 簇内路线段 + 景点。
    字体比主图小一号（×0.75）。

    Args:
        ax: 已创建并设好 extent 的 inset axes（PlateCarree 投影）
        extent_zoom: 放大区域范围
        cluster_indices: 簇内城市索引集合
        cities: 全部城市列表
        segments: 全部路线段列表
        day_colors: 天次颜色映射
    """
    from config import PROVINCE_COLORS, EN_PROV_MAP, PROV_CN_NAMES

    # 底图
    render_base_map(ax, extent_zoom)
    # 省界（在放大范围内）
    render_provinces(ax, extent_zoom, PROVINCE_COLORS,
                     EN_PROV_MAP, PROV_CN_NAMES)

    # 字体缩放
    zoom_font = 0.75
    font_city = 12   # 16 * 0.75
    font_prim = 10   # 14 * 0.75
    font_sec = 9     # 12 * 0.75

    # 簇内城市节点
    cluster_cities = {idx: cities[idx] for idx in cluster_indices}
    avg_lat = sum(c["lat"] for c in cluster_cities.values()) / len(cluster_cities) if cluster_cities else 45
    crs_cos_local = math.cos(math.radians(avg_lat))

    for idx, c in cluster_cities.items():
        # 城市椭圆
        r = CITY_RADIUS
        ax.add_patch(Ellipse(
            (c["lon"], c["lat"]), width=2 * r, height=2 * r * crs_cos_local,
            facecolor="white", edgecolor=c.get("color", "#888888"),
            linewidth=3, transform=ccrs.PlateCarree(), zorder=20,
        ))
        ax.text(c["lon"], c["lat"], c["name"],
                fontproperties=F(font_city), color=c.get("color", "#888888"),
                ha="center", va="center", zorder=21, path_effects=ST(3))

        # 景点直接绘制在真实坐标附近（简化布局）
        for is_prim, attr_key in [(True, "attractions_primary"),
                                   (False, "attractions_secondary")]:
            color = ATTR_PRIM_COLOR if is_prim else ATTR_SEC_COLOR
            sz = font_prim if is_prim else font_sec
            for a in c.get(attr_key, []):
                try:
                    alon, alat = float(a["lon"]), float(a["lat"])
                except (ValueError, TypeError):
                    continue
                # 打点
                ax.plot(alon, alat, "o", color=color, markersize=4,
                        markerfacecolor=color if is_prim else "none",
                        markeredgewidth=1.5,
                        transform=ccrs.PlateCarree(), zorder=16)
                # 标签：直接放在坐标旁
                ax.text(alon + 0.03, alat + 0.02, a["name"],
                        fontproperties=F(sz), color=color,
                        ha="left", va="bottom", zorder=17,
                        path_effects=ST(2))

    # 簇内路线段
    cluster_list = list(cluster_indices)
    for seg in segments:
        fi, ti = seg["from_index"], seg["to_index"]
        if fi in cluster_indices and ti in cluster_indices:
            day = seg["day"]
            color = day_colors.get(str(day), "#888888")
            render_route_segment(
                ax,
                cities[fi]["lon"], cities[fi]["lat"],
                cities[ti]["lon"], cities[ti]["lat"],
                color,
            )
```

- [ ] **Step 2: 验证语法**

```bash
python -c "from renderer import render_zoom_inset_content; print('OK')"
```

预期输出: `OK`

- [ ] **Step 3: Commit**

```bash
git add renderer.py
git commit -m "feat: 添加放大图内容渲染函数"
```

---

### Task 7: renderer.py — 主图上的放大标记

**Files:** Modify `renderer.py`

- [ ] **Step 1: 在 `render_zoom_inset_content` 下方添加 `render_zoom_indicator`**

```python
def render_zoom_indicator(ax_main: Any, extent_zoom: list,
                          inset_ax: Any, color: str = "#E74C3C") -> None:
    """在主图上绘制放大区域标记和连接线。

    包括：虚线矩形（标记放大范围）+ 两条连接线指向放大图。
    连接线从虚线框最近角连到放大图最近角。

    Args:
        ax_main: 主图 axes
        extent_zoom: 放大区域 [lon_min, lon_max, lat_min, lat_max]
        inset_ax: 放大图 axes（用于获取其位置）
        color: 标记颜色
    """
    # 虚线矩形标记放大范围
    lon_min, lon_max, lat_min, lat_max = extent_zoom
    rect_corners = [
        (lon_min, lat_min), (lon_max, lat_min),
        (lon_max, lat_max), (lon_min, lat_max), (lon_min, lat_min),
    ]
    ax_main.plot(
        [p[0] for p in rect_corners],
        [p[1] for p in rect_corners],
        color=color, linewidth=1.5, linestyle="--",
        transform=ccrs.PlateCarree(), zorder=98, alpha=0.7,
    )

    # 连接线：从虚线框最近角 → 放大图边框最近角
    # 使用混合坐标：放大图边框用 axes 坐标
    from matplotlib.transforms import blended_transform_factory
    fig = ax_main.figure

    # 找到放大区域在显示坐标中的位置
    disp_zoom_center = ax_main.transData.transform(
        ((lon_min + lon_max) / 2, (lat_min + lat_max) / 2))
    # 放大图 inset 在显示坐标中的位置
    inset_bbox = inset_ax.get_position()  # figure coords
    inset_center_fig = ((inset_bbox.x0 + inset_bbox.x1) / 2,
                        (inset_bbox.y0 + inset_bbox.y1) / 2)
    inset_center_disp = fig.transFigure.transform(inset_center_fig)

    # 在两个中心之间画一条淡色连线
    # 使用 ax_main 的数据坐标画连接线
    # 简化：在虚线框角落和 inset 角落之间画直线
    # 选择虚线框离 inset 最近的角
    corners_map = [(lon_min, lat_min), (lon_max, lat_min),
                   (lon_max, lat_max), (lon_min, lat_max)]
    # 找离 inset 中心最近的虚线框角
    best_corner = min(corners_map, key=lambda p:
        (p[0] - (lon_min + lon_max) / 2) * (inset_bbox.x0 + inset_bbox.x1 - 1) +
        (p[1] - (lat_min + lat_max) / 2) * (inset_bbox.y0 + inset_bbox.y1 - 1)
    )

    # 画两根短连线段，用 annotation 做
    ax_main.annotate(
        "", xy=(best_corner[0], best_corner[1]),
        xytext=((lon_min + lon_max) / 2 + 0.02, (lat_min + lat_max) / 2 + 0.02),
        arrowprops=dict(arrowstyle="-", color=color,
                       linewidth=1, alpha=0.6, ls="--"),
        transform=ccrs.PlateCarree(), zorder=97,
    )
```

- [ ] **Step 2: 验证语法**

```bash
python -c "from renderer import render_zoom_indicator; print('OK')"
```

预期输出: `OK`

- [ ] **Step 3: Commit**

```bash
git add renderer.py
git commit -m "feat: 添加主图放大区域标记渲染函数"
```

---

### Task 8: generate.py — 管道集成

**Files:** Modify `generate.py`

- [ ] **Step 1: 在 `generate()` 函数中，`_render_itinerary_and_title` 调用后插入放大图逻辑**

在 `_render_itinerary_and_title(ax, layout, cfg, all_days, day_to_segs, extent)` 之后，`plt.savefig` 之前，插入：

```python
    # L8: 局部放大图
    from config import INSET_THRESHOLD, INSET_PADDING

    close_pairs = _detect_close_pairs(cities, segments, INSET_THRESHOLD)
    if close_pairs:
        print(f"  检测到 {len(close_pairs)} 对过近城市")
        clusters = _group_connected_pairs(close_pairs)

        for ci, cluster in enumerate(clusters):
            extent_zoom = _compute_zoom_extent(cluster, cities, INSET_PADDING)
            if extent_zoom is None:
                continue

            cx, cy, hw, hh = _place_zoom_inset(
                layout, extent_zoom, extent, crs_cos)

            # 地图坐标 → 图形坐标，创建 inset axes
            from matplotlib.transforms import blended_transform_factory

            # 将地图坐标转为图形坐标
            disp_xy = ax.transData.transform((cx, cy))
            fig_xy = fig.transFigure.inverted().transform(disp_xy)

            # 放大图在图形坐标中的大小
            inset_fig_w = (2 * hw) / (extent[1] - extent[0]) * 0.98
            inset_fig_h = (2 * hh) / (extent[3] - extent[2]) * 0.98

            left = fig_xy[0] - inset_fig_w / 2
            bottom = fig_xy[1] - inset_fig_h / 2

            # 创建放大图 axes
            inset_ax = fig.add_axes(
                [left, bottom, inset_fig_w, inset_fig_h],
                projection=ccrs.PlateCarree(),
            )
            inset_ax.set_extent(extent_zoom)

            # 渲染放大图内容
            from renderer import (render_zoom_inset_content,
                                  render_zoom_indicator)
            render_zoom_inset_content(
                inset_ax, extent_zoom, cluster,
                cities, segments, cfg["day_colors"],
            )

            # 画边框
            import matplotlib.patches as mpatches
            inset_ax.add_patch(mpatches.Rectangle(
                (0, 0), 1, 1, transform=inset_ax.transAxes,
                facecolor="none", edgecolor="#E74C3C",
                linewidth=2, zorder=100,
            ))

            # 主图标记
            render_zoom_indicator(ax, extent_zoom, inset_ax)

            print(f"  放大图{ci+1}: {sorted(cluster)} -> "
                  f"extent=({extent_zoom[0]:.2f},{extent_zoom[1]:.2f},"
                  f"{extent_zoom[2]:.2f},{extent_zoom[3]:.2f})")
```

- [ ] **Step 2: 运行北疆配置验证**

```bash
python generate.py routes/beijiang_config.json
```

预期输出: 控制台显示"检测到 1 对过近城市"和放大图信息，生成成功，无崩溃。

- [ ] **Step 3: 检查输出文件中的放大图**

打开 `~/.hermes/cache/documents/` 下生成的 PNG，确认：
- 虚线矩形标记了喀纳斯-禾木区域 ✓
- 放大图出现在无冲突的空白区域 ✓
- 放大图内底图 + 城市 + 路线 + 景点完整 ✓
- 无重叠或被截断 ✓

- [ ] **Step 4: 验证其他路线不受影响**

```bash
python generate.py routes/nanjiang_config.json
python generate.py routes/xian_config.json
```

预期: 两条路线都没有过近城市对（或正常跳过），输出正常。

- [ ] **Step 5: Commit**

```bash
git add generate.py
git commit -m "feat: 集成局部放大图到生成管道"
```

---

### Task 9: 优化 — render_zoom_indicator 连接线改进

**Files:** Modify `renderer.py`

- [ ] **Step 1: 重写 `render_zoom_indicator` 使用真实的角到角连线**

```python
def render_zoom_indicator(ax_main: Any, extent_zoom: list,
                          inset_ax: Any, color: str = "#E74C3C") -> None:
    """在主图上绘制放大区域标记和连接线。

    虚线矩形标记放大范围 + 从矩形角到放大图边框角的连接线。
    """
    lon_min, lon_max, lat_min, lat_max = extent_zoom

    # 虚线矩形
    rect_lons = [lon_min, lon_max, lon_max, lon_min, lon_min]
    rect_lats = [lat_min, lat_min, lat_max, lat_max, lat_min]
    ax_main.plot(rect_lons, rect_lats, color=color, linewidth=1.5,
                 linestyle="--", transform=ccrs.PlateCarree(),
                 zorder=98, alpha=0.7)

    # 获取 inset axes 在图形坐标中的边框四角
    fig = ax_main.figure
    inset_bbox = inset_ax.get_position()

    # 选择连线角：虚线框离 inset 最近的角
    corners_map = [(lon_min, lat_min), (lon_max, lat_min),
                   (lon_max, lat_max), (lon_min, lat_max)]
    inset_corners_fig = [
        (inset_bbox.x0, inset_bbox.y0),
        (inset_bbox.x1, inset_bbox.y0),
        (inset_bbox.x1, inset_bbox.y1),
        (inset_bbox.x0, inset_bbox.y1),
    ]

    # 找最近的角对
    import numpy as np
    pairs = []
    for ci, (mlon, mlat) in enumerate(corners_map):
        mx, my = ax_main.transData.transform((mlon, mlat))
        mxf, myf = fig.transFigure.inverted().transform((mx, my))
        for ii, (ixf, iyf) in enumerate(inset_corners_fig):
            dist = math.hypot(mxf - ixf, myf - iyf)
            pairs.append((dist, ci, ii, mlon, mlat, ixf, iyf))

    pairs.sort()
    used_map = set()
    used_inset = set()
    for _, ci, ii, mlon, mlat, ixf, iyf in pairs:
        if ci not in used_map and ii not in used_inset:
            used_map.add(ci)
            used_inset.add(ii)
            # 地图坐标 → 图形坐标 → 用 annotation 画线
            ax_main.annotate(
                "", xy=(ixf, iyf), xycoords="figure fraction",
                xytext=(mlon, mlat), textcoords=ccrs.PlateCarree(),
                arrowprops=dict(arrowstyle="-", color=color,
                               linewidth=1, alpha=0.5, ls="--"),
                zorder=97,
            )
        if len(used_map) >= 2:
            break
```

- [ ] **Step 2: 重新生成北疆图验证连线效果**

```bash
python generate.py routes/beijiang_config.json
```

审视输出：连线应清晰、不与其他元素重叠。

- [ ] **Step 3: Commit**

```bash
git add renderer.py
git commit -m "fix: 改进放大图连接线为角到角最短连线"
```

---

### Task 10: 最终测试

**Files:** 无新建，验证全部路线

- [ ] **Step 1: 生成所有路线**

```bash
python generate.py --all
```

- [ ] **Step 2: 确认**
   - 北疆：有放大图（喀纳斯-禾木）
   - 南疆、老君山、西安：正常无放大图（无过近对）
   - 所有路线不崩溃

- [ ] **Step 3: 打 tag 并最终 commit**

```bash
git add VERSION
# 如果 VERSION 需要更新则更新
git commit -m "v0.2.0: 添加自动局部放大图功能"
```

---

## 回退策略

- 检测到 0 个过近对 → 跳过全部放大逻辑，主图不受影响
- 螺旋搜索找不到位置 → 回退到右上角
- 放大图渲染异常 → try/except 包裹，失败时只打日志不崩溃
- 景点坐标无效 → `_compute_zoom_extent` 跳过无效坐标
